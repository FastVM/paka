
def findvar(vars, name) {
    i = 0
    while i < length(vars) {
        k = vars[i]
        i = i + 1
        v = vars[i]
        i = i + 1
        if str.eq(k, name) {
            return [v]
        }
    }
    return []
}

def findvar.ikey(vars, name) {
    i = 0
    while i < length(vars) {
        k = vars[i]
        i = i + 1
        v = vars[i]
        i = i + 1
        if k == name {
            return [v]
        }
    }
    return []
}
