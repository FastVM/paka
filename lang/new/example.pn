syntax 'unroll var 'until value body {    
    syntax if (value != 0) {
        unroll var until {
            syntax eval (value - 1)
        } body
    }
    var = value; body
}

unroll x until 100 {
    print x
}