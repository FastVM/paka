def abc() {
    x = 1
    def l() {
        return x
    }
    one = json.dump(l)
    x = 2
    two = json.dump(l)
    return [one, two]
}

[one, two] = abc()
io.print(json.undump(one)())
io.print(json.undump(two)())