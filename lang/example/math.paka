
def avg(arr) {
    save = 0
    index = 0
    while index < length(arr) {
        save = save + arr[index]
        index = index + 1
    }
    return save / length(arr)
}

midf = "hello world" |> avg

mid = midf - midf % 1

putchar(mid)
putchar(10)
