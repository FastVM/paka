
if (arr.len(args) != 0) {
    json = io.slurp(args[0]);
    io.print("(using world: " ~ args[0] ~ ")");
    _serial_thaw(json);
};

io.print("welcome to paka repl!");
io.print();

while (true) {
    code = io.readln("paka> ");
    res = sys.eval(code);
    state = _serial_freeze();
    io.dump("repl.json", state);
    if (res != nil) {
        io.print(res);
    };
};