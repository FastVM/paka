def deep(n, f) {
    return if (n > 0) {
        deep(n-1, f);
    } else {
        f();
    };
};

deep(10000) {
    x = 0;
    cont = sys.callcc(cc => cc);
    x += 1;
    if (x < 4500000) {
        cont(cont);
    };
    io.print(x);
};
