pow2 = {};

def times(n, f) {
    arr.each(arr.range(n), f);
};

pow2[0] = 1;

times(256 * 256) {
    pow2[$0+1] = pow2[$0]*2;
};

io.print(tab.len(pow2));
tab.each(pow2) {
    io.print($0, ": ", $1);
};
