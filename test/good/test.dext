io.print("should print multiples of 6");

arr.each(arr.filter(
    arr.map(arr.range(1, 10 * 2 + 1)) {
        $0 * 3;
    }) {
        $0 % 2 == 0;
}) {
    io.print($0);
};

io.print("should have printed multiples of 6");