io.print("BEGIN");

def slow(n) {
    while (n > 0) {
        n -= 1;
    };
};

def fib(n) {
    return if (n < 2) {
        n;
    } else {
        fib(n-2) + fib(n-1);
    };
};

arr.each(arr.range(16)) {
    # slow(256 * 256 * 64);
    fib(27);
};

io.print("END");